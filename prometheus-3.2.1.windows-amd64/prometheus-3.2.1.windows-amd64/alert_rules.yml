groups:
  - name: example_alert
    rules:
      - alert: HighCPUUsage
        expr: process_cpu_seconds_total > 0.5
        for: 1m
        labels:
          severity: warning
        annotations:
          description: "High CPU usage detected!"
